# Copy to .env.local and fill in your own key.
# IMPORTANT: Do not commit real API keys.

# --- Server (AI proxy + Auth) ---
# The Express server runs on PORT and serves:
# - /api/evaluate-reflection
# - /auth/verify

PORT=8787

# Comma-separated list of allowed frontend origins (for production).
# In dev, localhost is always allowed.
FRONTEND_ORIGINS=http://localhost:3000

# --- WordPress JWT SSO ---
# Provide either WP_JWT_SECRET (HS256) or WP_JWT_PUBLIC_KEY (RS256).
# If set, the AI endpoint will also require a valid member token.

WP_JWT_SECRET=
# WP_JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# Optional hardening:
WP_JWT_ISSUER=
WP_JWT_AUDIENCE=

GEMINI_API_KEY=

# OpenAI (optional)
# If set, the AI proxy will prefer OpenAI over Gemini.
OPENAI_API_KEY=
# Example models: gpt-4o-mini, gpt-4.1-mini (depending on your account access)
OPENAI_MODEL=gpt-4o-mini

# --- Frontend (Vite) ---
# Optional base URL for the API server. Defaults to http://localhost:8787
VITE_AI_PROXY_URL=http://localhost:8787

# Optional: URL to start the WP SSO login flow.
# Expected to redirect back to the app with ?token=... (JWT).
VITE_WP_SSO_URL=
